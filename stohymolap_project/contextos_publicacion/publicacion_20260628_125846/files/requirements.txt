# Dependencias minimas (nucleo del pipeline)
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
pyyaml>=6.0
joblib>=1.3
tabulate>=0.9        # para best_model_summary.md (to_markdown)

# Opcionales: si estan instalados, el pipeline los usa automaticamente.
# Si no, hay fallback a scikit-learn (HistGradientBoosting / MLP).
# xgboost>=2.0
# tensorflow>=2.13   # backend GRU
# torch>=2.0         # backend GRU alternativo
